import type { NextConfig } from "next";

const nextConfig: NextConfig = {
 // next.config.js
  images: {
    domains: ["images.pexels.com", "www.pexels.com", "source.unsplash.com","plus.unsplash.com", "images.unsplash.com"],
  },

};

export default nextConfig;
